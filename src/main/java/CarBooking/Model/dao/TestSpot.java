package CarBooking.Model.dao;

import CarBooking.Model.Spot;

import java.sql.SQLException;
import java.util.List;

public class TestSpot {
    public static void main(String[] args) throws SQLException, ClassNotFoundException {
        SpotDBManager spotDBManager = new SpotDBManager();

//        Spot testSpot = new Spot("UTS", 1.5, 10.0, 14.0, 20, 100, 500, 1, 0);
//        spotDBManager.addSpot(testSpot);

//        spotDBManager.deleteSpot(2);

//        Spot testSpot = new Spot(2, "UTS", 1.5, 10.0, 1400.0, 20, 100, 500, 1, 0);
//        spotDBManager.adjustSpot(testSpot);

//        List<Spot> spots = spotDBManager.readAllSpot();
//        System.out.println(spots.get(0).getId());

//        List<Spot> spots = spotDBManager.filterByType(1, 0);
//        System.out.println(spots);

    }
}
